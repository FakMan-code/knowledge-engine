# Layer 1 (ingesta) runs on the standard library alone.
# Anything listed here is optional and only widens what can be extracted.
# A missing dependency is never a crash: the document is recorded in the
# ingest ledger as not extracted, with the reason, and surfaces as a gap.

# -- PDF -----------------------------------------------------------------
# pypdf>=4.0

# -- Office --------------------------------------------------------------
# python-docx>=1.1
# openpyxl>=3.1
# python-pptx>=0.6

# -- OCR (also needs the tesseract binary on PATH) -----------------------
# pytesseract>=0.3.10
# Pillow>=10.0
