"""Layer 1: whatever the organisation has becomes addressable text with a span.

Deterministic on purpose. No model runs here, so two people ingesting the same
folder get byte-identical results and a citation never depends on what a model
felt like saying that day.
"""
